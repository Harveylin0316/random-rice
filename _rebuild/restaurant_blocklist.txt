# 強制 enabled=false 的 or_id 清單（完全從推薦池移除）
# 每行一個 or_id（純數字），# 開頭是註解
#
# 用途：手動排除 OR 線上頁面內容不完整、體驗很差的店
# 自動規則「空殼餐廳」（無圖+無評分+無 type+無 cuisine）已會抓掉一批，
# 如果有店符合自動規則但 OR 頁面其實 OK，可以從這裡留註解但不加 ID 略過；
# 反之，自動規則沒抓到但 OR 頁面其實壞掉的店，加 ID 到這裡。
#
# 加完後重跑 _rebuild/10_merge_external_xlsx.py 即可生效

# 範例（已被自動規則 empty-shell 涵蓋，不需重複加，僅做文檔）
# 532257  # 猿樂燒肉丼（OR 頁面幾乎空白）
