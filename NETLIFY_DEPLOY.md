# Netlify 部署指南

## 📋 部署步驟

### 1. 在 Netlify 上設置專案

1. 登入 [Netlify](https://app.netlify.com/)
2. 點擊「Add new site」→「Import an existing project」
3. 選擇「GitHub」並授權
4. 選擇你的 `random-rice` 專案
5. **重要**：Netlify 會自動讀取 `netlify.toml` 配置文件，所以大部分設置已經完成

### 2. 構建設置（可選，通常不需要手動設置）

如果 Netlify 沒有自動讀取 `netlify.toml`，可以在「Site settings」→「Build & deploy」中手動設置：

- **Base directory**: （留空，使用根目錄）
- **Build command**: （留空，因為前端是靜態文件）
- **Publish directory**: `frontend`

### 3. 環境變數（如果需要）

如果你的 Functions 需要環境變數（例如 API keys），在 Netlify 的「Site settings」→「Environment variables」中添加：

- `GOOGLE_API_KEY`: （如果需要，但通常不需要，因為資料已經在資料庫中）

### 4. 部署

完成設置後，Netlify 會自動：
1. 從 GitHub 拉取最新代碼
2. 安裝依賴（如果有 `package.json`）
3. 構建 Functions
4. 部署前端文件

### 5. 驗證部署

部署完成後，訪問你的 Netlify URL（例如：`https://your-site.netlify.app`），測試以下功能：

- ✅ 前端頁面正常載入
- ✅ 獲取位置功能正常
- ✅ 篩選選項正常載入
- ✅ 推薦餐廳功能正常
- ✅ 地區選擇功能正常

## 🔧 本地測試 Netlify Functions

如果你想在本地測試 Netlify Functions，可以安裝 Netlify CLI：

```bash
npm install -g netlify-cli
```

然後在專案根目錄運行：

```bash
netlify dev
```

這會啟動本地開發服務器，模擬 Netlify 環境。

## 📝 注意事項

1. **資料庫文件位置**：`restaurants_database.json` 必須在專案根目錄，這樣 Functions 才能正確讀取。

2. **API 路徑**：前端已經配置為自動檢測環境，在 Netlify 上會使用相對路徑 `/api`。

3. **CORS**：Netlify Functions 已經配置了 CORS headers，允許跨域請求。

4. **文件大小限制**：Netlify Functions 有執行時間和內存限制。如果資料庫很大，可能需要優化。

## 🐛 常見問題

### 問題：Functions 找不到資料庫文件

**解決方案**：確保 `restaurants_database.json` 在專案根目錄，並且已經提交到 GitHub。

### 問題：API 請求返回 404

**解決方案**：檢查 `netlify.toml` 中的重定向規則是否正確。

### 問題：CORS 錯誤

**解決方案**：確保 Functions 返回正確的 CORS headers（已在代碼中配置）。

## 📚 相關文件

- `netlify.toml` - Netlify 配置文件
- `netlify/functions/restaurants.js` - Netlify Function 處理 API 請求
- `frontend/app.js` - 前端代碼（已更新 API URL）
